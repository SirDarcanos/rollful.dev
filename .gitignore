node_modules/
dist/
.wrangler/
.astro/
.dev.vars
*.tgz
.DS_Store

# Regenerated by `wrangler types`, which every typecheck runs first.
worker-configuration.d.ts

# Local overrides. `.env.production` is committed on purpose: it is what stops a local
# `.env` from being baked into a deployed build.
.env
