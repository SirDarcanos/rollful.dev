{
  "name": "@rollful/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Rollful dice-rolling API, deployed as a Cloudflare Worker.",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "wrangler types && tsc --noEmit",
    "test": "vitest run",
    "openapi": "node scripts/write-openapi.mjs"
  },
  "dependencies": {
    "@hono/zod-openapi": "^1.5.1",
    "@rollful/schema": "*",
    "hono": "^4.13.0",
    "opendice": "^1.3.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.119.0"
  }
}
