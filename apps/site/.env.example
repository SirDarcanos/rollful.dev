# Which API the home page roller talks to.
# Unset, the site uses production. Copy this file to .env to point at a local Worker
# started with `npm run dev --workspace @rollful/api`.
PUBLIC_ROLLFUL_API=http://localhost:8787
