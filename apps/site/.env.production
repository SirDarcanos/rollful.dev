# Loaded only by `astro build`, and it beats `.env`. Without this, building on a machine
# that has a local `.env` bakes that developer's localhost into the deployed site.
PUBLIC_ROLLFUL_API=https://api.rollful.dev
